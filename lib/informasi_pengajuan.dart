import 'package:flutter/material.dart';

class InformasiPengajuan extends StatelessWidget {
  const InformasiPengajuan({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [
        Text('Informasi Pengajuan'),
        Placeholder(),
      ],
    );
  }
}
