import 'package:flutter/material.dart';

class StatusPengajuan extends StatelessWidget {
  const StatusPengajuan({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [
        Text('Status Pengajuan'),
        Placeholder(),
      ],
    );
  }
}